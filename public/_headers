# Cloudflare Pages headers. Hashed build assets never change → cache forever.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# The service worker and HTML must stay fresh so updates roll out.
/sw.js
  Cache-Control: no-cache
/index.html
  Cache-Control: no-cache
